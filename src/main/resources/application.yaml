spring:
  application:
    name: url_monitor

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
server:
  port: 5000
jwt:
  secret: ${JWT_SECRET}